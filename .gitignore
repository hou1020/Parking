__pycache__/
*.py[cod]
*$py.class

.idea/
*.tif.aux.xml

# Licensed Digimap / Getmapping aerial imagery — must never be committed.
# Already-tracked files are NOT affected by these rules; they must be removed
# from the index and from history separately.
*.tif
*.tiff
